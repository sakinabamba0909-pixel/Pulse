'use client';

export default function CalendarClient({ tasks, projects, calendarConnected }) {
  return <div style={{ padding: 60, fontFamily: "'Outfit', sans-serif" }}><p>Calendar — loading...</p></div>;
}
